# SaleeM V7.3 — قواعد الأسهم والشموع المتوقعة المرتبطة بالهيكل الحقيقي

هذه النسخة تُكمل V7.2 وتُطبّق قواعد الرسم المعتمدة على الشارت المعاد بناؤه من OHLC الحقيقي، بدون اختلاق مستويات أو نقاط تثبيت.

## ما تم تنفيذه

1. **TP1 / TP2 / TP3**
   - خطة التنفيذ المؤكدة/المشروطة تستخدم الأهداف الحقيقية التي أعادها المحلل.
   - كل هدف يُرسم عند سعره الحقيقي ويظهر كبطاقة على المحور اليميني.
   - لا يتم إنشاء TP2 أو TP3 اصطناعيين إذا لم يرجعهما التحليل.
   - الصفقة المؤكدة تبقى فعالة بعد TP1 وTP2، وتُعتبر مكتملة عند آخر هدف متاح.

2. **الشموع المتوقعة**
   - تبدأ بعد Entry.
   - تشرح بصريًا التسلسل: `BREAK → RETEST → CONTINUATION`.
   - تمر على TP1 ثم TP2 ثم TP3 عند توفرها.
   - هي شرح لمسار السيناريو ولا تُعامل كشموع سوق فعلية.

3. **قواعد خطوط الأسهم**
   - كل سهم سيناريو يبدأ من سعر **Entry** نفسه.
   - المسار الأساسي يحتوي انكسارات مقصودة لشرح: كسر → إعادة اختبار → استمرار.
   - في الحالة المؤكدة يظهر مسار التنفيذ الأساسي فقط.
   - في حالة WATCH الغامضة فقط يمكن عرض معاينة مسارين صالحين: اختراق صاعد ورفض/كسر هابط، وكلاهما يبدأ من التفعيل الحقيقي الخاص به.
   - لا يتم عرض أكثر من صفقة نشطة واحدة.

4. **قسم ARROW RULES أسفل الشارت**
   - UP = breakout.
   - DOWN = rejection / break.
   - PATH = BREAK → RETEST → CONTINUATION.
   - ORIGIN = all scenario arrows start at ENTRY.

5. **بوابة التثبيت الحقيقي لعناصر SMC**
   - لا يُرسم Liquidity Sweep أو BOS أو MSS/CHOCH أو OB أو FVG إلا بوجود:
     - `anchor_index` حقيقي.
     - `price_level` حقيقي.
     - `validation_reason` يشرح سبب التحقق.
   - المناطق تحتاج كذلك حدود سعر حقيقية.

6. **Liquidity Sweep**
   - مرتبط بذيل حقيقي تجاوز Pivot/Equal Highs/Equal Lows ثم عاد حسب شرط السحب.
   - خطه يبدأ من مرساة السيولة الحقيقية وليس من موضع بصري عشوائي.

7. **BOS / MSS-CHOCH**
   - حدث الهيكل يحمل القمة/القاع المكسور، شمعة الكسر، إغلاق الكسر، وسبب التحقق.
   - MSS/CHOCH لا يظهر في الرسم أو الـLegend عندما لا يوجد حدث حقيقي صالح.

8. **Order Block / FVG**
   - OB = آخر شمعة معاكسة صحيحة قبل الاندفاع الحقيقي.
   - FVG = فجوة ثلاث شموع حقيقية وحدودها مأخوذة من OHLC.
   - السيناريو الهابط لا يستعير منطقة صاعدة والعكس لمجرد ملء الرسم.

9. **عدم تحريك الهندسة عند الازدحام**
   - نقاط التثبيت والأسعار والخطوط والمناطق تبقى في مكانها الحقيقي.
   - الذي يمكن تحريكه بصريًا هو النص/الليبل فقط.

10. **M5 Trigger وScenario Freshness**
    - لا تُعاد صفقة قديمة بعد اكتمالها كخطة جديدة.
    - الخطة المشروطة تتطلب Trigger جديدًا على M5.
    - الخطة المؤكدة لا تنتهي لمجرد وصول TP1؛ تتابع حتى آخر هدف حقيقي متاح.

## ملفات التنفيذ الرئيسية

- `app/engine/renderer.py`
- `app/engine/reference_scenario_engine.py`
- `app/services/analyzer.py`
- `tests/test_smc_real_chart_v73_arrow_rules.py`

## التحقق

تمت إضافة اختبارات خاصة بـ V7.3 للتحقق من:
- الأهداف الثلاثة.
- استمرار دورة الصفقة بعد TP1/TP2.
- بدء السهم من Entry حرفيًا.
- وجود Break/Retest bend في المسار.
- معاينة المسارين في WATCH فقط.
- وجود بيانات التثبيت والسعر وسبب التحقق في هندسة SMC.
