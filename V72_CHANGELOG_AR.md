# SaleeM V7.2 — Current Price & Scenario Freshness

هذا التحديث يعالج التعارض الذي ظهر عند مقارنة صورة MT5 الأصلية بنتيجة SaleeM.

## التعديلات

- السعر الحالي لم يعد مقفولًا على آخر إغلاق M5 فقط. يستخدم SaleeM سعر السوق الحي كمرجع، ويقبل سعر بطاقة الشارت المرفوع إذا كان قريبًا منه ضمن بوابة أمان تعتمد على ATR.
- لا يتم تحريك الشموع أو الدعم/المقاومة أو BOS/CHOCH أو OB/FVG أو نقاط النموذج لتلائم الصورة. قفل الصورة يخص السعر الحالي فقط.
- إضافة Scenario Freshness Guard: إذا وصل السعر الحالي إلى الهدف السابق أو تجاوزه، يمنع عرض Entry/Target القديم كفرصة جديدة ويحوّل الحالة إلى مراقبة وإعادة تقييم.
- السيناريو الذي ابتعد السعر كثيرًا عن تفعيله يُصنف Retest Only بدل مطاردة السعر.
- عند انتهاء الخطة، تختفي بطاقات Entry/Target القديمة من المحور ويظهر CURRENT فقط، مع إلغاء صندوق المخاطرة والمسار التنفيذي القديم.
- العنوان يتحول عند انتهاء الخطة إلى WATCH / RE-EVALUATE بدل استمرار عرض BULLISH/BEARISH REVERSAL كصفقة جديدة.
- الرسم النهائي في التطبيق يركز افتراضيًا على آخر 42 شمعة M5، بينما يظل التحليل الداخلي قادرًا على استخدام تاريخ أطول. العناصر الهندسية خارج نافذة العرض لا تُسحب أو تُثبت على شمعة خاطئة.
- مقياس السعر في النتيجة يعتمد على الشموع المرئية والخطة الحية فقط؛ الأهداف القديمة أو المستويات البعيدة لا توسع المحور بشكل مصطنع.
- تحسين توافق `app/main.py` مع Python 3.9 باستخدام `Optional[...]` بدل `X | None` في مواضع FastAPI/Pydantic.

## تحقق

تم تشغيل مجموعة الاختبارات الكاملة بعد التعديل:

`214 passed`
