# SALEEM_EDUCATIONAL_OVERLAY_SPEC_V367

## القاعدة العليا

`final_result = original_uploaded_chart_pixels + verified_educational_overlay`

لا يعيد SaleeM رسم الشموع ولا ينشئ شارتًا بديلًا. بيانات السوق تؤكد البنية والسيناريو؛ الصورة الأصلية تحدد موضع الرسم النهائي.

## خط التنفيذ

1. حفظ الصورة الأصلية بلا Crop أو Stretch.
2. قراءة حدود منطقة الشارت ومحور السعر والسعر الحالي عند الإمكان.
3. جلب H4/H1/M15/M5 للتحقق من الاتجاه والبنية والتفعيل.
4. مطابقة الصورة مع أطلس النماذج والسيناريوهات.
5. قبول عائلة/سيناريو واحد فقط إذا أكدها المحرك الحتمي.
6. استخراج إحداثيات بصرية للنقاط والخطوط والمناطق المرئية.
7. السماح بالهندسة البصرية فقط إذا `visual_geometry_score >= 68` وبعد تطابق المرجع مع السيناريو/العائلة المقبولة.
8. إنشاء Overlay منفصل ثم دمجه فوق الصورة الأصلية.
9. وضع شرح القاعدة أسفل الشارت في Save/Share.

## عناصر الرسم حسب الأولوية

1. هندسة النموذج: W/M/H&S/Triangle/Wedge/Channel/...
2. مستوى التفعيل أو Entry عند اكتمال القاعدة.
3. Invalidation/Stop.
4. سهم السيناريو المتوقع.
5. Target.
6. BOS/CHOCH/MSS/IDM.
7. Order Block/FVG/Liquidity عند ارتباطها مباشرة بالسيناريو.
8. التسميات المساندة.

عند الازدحام تحذف العناصر الأدنى أولوية؛ لا تتحرك نقاط الارتكاز أو الخطوط عن مواضعها الحقيقية.

## الحالات

- `candidate`: النموذج مثبت لكنه ينتظر الكسر/التفعيل؛ الخطوط والسهم متقطعان ولا تُعرض صفقة تنفيذية كاملة.
- `confirmed`: اكتملت قاعدة السيناريو؛ الخطوط والسهم متصلان، وتظهر Entry/Stop/TP فقط إذا كانت هندسة التنفيذ ومحور السعر موثوقين.
- `none`: لا نموذج ولا سهم ولا مناطق صفقة وهمية.

## معايرة السعر

- Pixel calibration من خط السعر الحالي + شبكة الشارت هو الخيار الأول.
- عند فشله يسمح فقط بالاستيفاء بين أرقام محور حقيقية مقروءة من الصورة.
- عند غياب معايرة موثوقة تُخفى العناصر المرتبطة بسعر مثل Entry/Stop/TP؛ لا تُرسم عند ارتفاع تخميني.

## هندسة الصورة

المطابقة البصرية يجوز أن تعيد نقاطًا وخطوطًا بنسب 0..1 من الصورة الكاملة، لكنها لا تقرر صفقة. يسمح باستخدامها فقط بعد تحقق نفس العائلة/السيناريو بواسطة المحرك الحتمي. كل نقطة يجب أن تقع داخل `chart_plot_bounds`.

## السهم

السهم إلزامي للسيناريو المقبول. يبدأ من آخر نقطة حقيقية/منطقة تفعيل مرئية. مرشح = متقطع، مؤكد = متصل. لا يوجد سهم عند غياب سيناريو صالح.

## الصورة الأصلية

`base_layer` غير قابل للاستبدال. يسمح فقط بإنشاء `overlay_layer` ثم Composite. لا تُستخدم مولدات صور أو شموع بديلة لصناعة Background.

## دعم الاتجاه

Portrait / Square / Landscape كلها صالحة. الرسم يلتزم بحدود `chart_plot_bounds` ولا يخرج إلى هيدر المنصة أو أزرار الهاتف.

## الذاكرة المرجعية

السيناريو الأقرب يأتي من ذاكرة SaleeM المرجعية المعتمدة. المطابقة البصرية للترتيب والتموضع، والتحقق الحتمي هو بوابة السماح بالرسم. نموذج واحد أساسي هو الوضع الافتراضي.
