# SaleeM v3.50 — Strict structure and pattern execution overlay

- Original uploaded chart remains untouched.
- X coordinates are accepted only from wick/price-matched screenshot candles. No trailing-window fallback.
- W/M core is exactly three real pivots plus the real breakout candle when confirmed.
- BOS/CHOCH are closed-candle breaks of known swing levels. IDM is an actual internal opposite swing inside the latest BOS leg.
- Only primary pattern can show Entry/Activation/Invalidation/forecast. Secondary pattern is geometry only.
- Execution overlay requires active market, conditional/confirmed draw mode, and direction/action matching pattern bias.
- Closed/watch/conflicting scenarios hide execution path instead of guessing.
